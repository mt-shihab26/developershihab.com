---
import { getArticles } from "~/lib/articles";

import SiteLayout from "~/components/layouts/site-layout/index.astro";

const articles = (await getArticles()).slice(0, 2);
---

<SiteLayout
    title="Shihab Mahamud - Full-stack developer"
    description="Software developer from bangladesh. I mainly work with Laravel, React & WordPress."
>
    <h1 class="mb-4 text-2xl font-extrabold text-black">Articles</h1>
    <div class="space-y-6 text-lg leading-normal">
        <p>Over the years I’ve published a few dozen articles — some more noteworthy than others.</p>
        <p>Here are some of my personal favorites.</p>
    </div>
    <div class="mt-12 space-y-10">
        {
            articles.map((article) => (
                <div>
                    <div>
                        <a href={article.slug} class="text-lg font-bold text-black no-underline hover:underline">
                            {article.data.title}
                        </a>
                    </div>
                    <p class="mt-1 text-base leading-normal">{article.data.description}</p>
                    <div class="mt-2 text-base leading-normal">
                        <a href={article.slug} class="text-sm no-underline hover:text-black hover:underline">
                            Read this article &rarr;
                        </a>
                    </div>
                </div>
            ))
        }
    </div>
    <div class="mt-12 space-y-6 text-lg leading-normal">
        <p>
            Read the rest of my articles in the <a href="/articles/archives" class="text-black underline">archives</a>.
        </p>
    </div>
</SiteLayout>
