---
import { cn } from '@/utils/tailwind';

const { class: className, href, ...props } = Astro.props;
---

<a href={href} class={cn('font-semibold text-foreground', className)} {...props}>
	<slot />
</a>
